<section>
    <h3>{{title}}</h3>
</section>

<section>
    {{#if admin}}
        <a href=/manage><button>ManageDb</button></a>
    {{/if}}
</section>

<section>
    <input id="search" type="text" placeholder="Search here...">
    <button onclick="search();">SEARCH</button>
    <br><br>

    <table class="users" cellpadding="10">
        <tr>
            <td align="left">NAME</td>
            <td align="left">ID</td>
            <td align="center">OWNED</td>
        </tr>

        {{#each movies}}
            <tr>
                <td align="left">{{name}}</td>
                <td align="center">{{ID}}</td>
                <td align="center">{{owned}}</td>
                <td align="center"><button onclick="toggleOwned();">Add/Remove</button></td>
            </tr>
        {{/each}}
    </table>
</section>

<script>
    function search() {
        let val = $('#search').val().trim();

        if (val && val.length > 2) {
            window.location = '/manage/find/' + val;
        }
        else {
            alertify.alert('Hey!', 'Please type a minimum of 3 characters into the search field!', null);
        }
    }
    function toggleOwned() {
        //will need to dispatch a post request with the context name
        //need to figure out how to get the correct movie information from the button
        console.log('toggled');
        console.log(this);
    }
</script>
